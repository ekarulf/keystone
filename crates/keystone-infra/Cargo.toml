[package]
name = "keystone-infra"
description = "CDK project generation and profile synchronization for Keystone"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
keystone-core.workspace = true
keystone-pki.workspace = true

serde.workspace = true
serde_json.workspace = true
tera.workspace = true
time.workspace = true
toml.workspace = true

[dev-dependencies]
# The plan tests need a real CA certificate to offer as the trust anchor bundle,
# and a device certificate to prove one is refused there.
keystone-pki = { workspace = true, features = ["testing"] }

[lints]
workspace = true
